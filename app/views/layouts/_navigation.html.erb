<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "My schedule", Student.find_by(email: current_user.email), class: 'navbar-brand' %>
      <%= form_tag students_path, method: "get", class: "navbar-form navbar-left navbar-search" do %>
      <!-- May God help me for the code below -->
        <div class="input-group"><%= text_field_tag :search, nil, placeholder: "Search", class: "form-control" %><span class="input-group-btn"><button class="btn btn-default" name="commit" type="submit"><span class="glyphicon glyphicon-search"></span></button></span></div>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
    	<% if user_signed_in? %>
    	  <li><p class="navbar-text">Signed in as <%= current_user.email.split('@')[0] %></p></li>
    	  <li><%= link_to "Account settings", edit_user_registration_path %></li>
          <li><%= link_to "Sign out", destroy_user_session_path, method: "delete" %></li>
    	<% end %>
      </ul>
    </div>
  </div>
</nav>
